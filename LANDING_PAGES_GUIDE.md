# 📚 دليل استخدام صفحات الهبوط (Landing Pages)

## 🎯 نظرة عامة

تم إنشاء 3 قوالب احترافية لصفحات الهبوط (Landing Pages) متوافقة تماماً مع هوية NawaEduTech:

1. **PrintedProductLanding** - للمنتجات المطبوعة
2. **DigitalProductLanding** - للمنتجات الرقمية
3. **CourseLanding** - للدورات التدريبية

---

## 🌐 كيفية الوصول للصفحات

بعد تشغيل السيرفر (`npm run dev`), يمكنك معاينة الصفحات على:

- **المنتجات المطبوعة**: `http://localhost:5173/landing/printed-product`
- **المنتجات الرقمية**: `http://localhost:5173/landing/digital-product`
- **الدورات**: `http://localhost:5173/landing/course`

---

## ✏️ كيفية التعديل على المحتوى

### 📝 الخطوة 1: افتح الملف المناسب

- للمنتجات المطبوعة: `src/pages/PrintedProductLanding.tsx`
- للمنتجات الرقمية: `src/pages/DigitalProductLanding.tsx`
- للدورات: `src/pages/CourseLanding.tsx`

### 📝 الخطوة 2: ابحث عن قسم PRODUCT_DATA أو COURSE_DATA

في بداية كل ملف، ستجد قسم مُعلّم بوضوح:

```typescript
// ============================================
// 📝 EDIT THIS SECTION FOR YOUR PRODUCT
// ============================================
const PRODUCT_DATA = {
  // هنا تضع بياناتك
}
```

### 📝 الخطوة 3: عدّل البيانات حسب منتجك

---

## 📦 1. صفحة المنتج المطبوع (PrintedProductLanding)

### البيانات الأساسية:

```typescript
const PRODUCT_DATA = {
  // العنوان والوصف
  title_ar: 'مخطط أسبوعي للمعلم',  // غيّر هذا
  title_en: 'Teacher Weekly Planner',
  subtitle_ar: 'نظّم أسبوعك التعليمي بذكاء واحترافية',
  
  // السعر
  price: 1500,  // السعر بالدينار الجزائري
  originalPrice: 2000,  // السعر الأصلي (اختياري للخصم)
  currency: 'د.ج',
  
  // الصورة الرئيسية
  heroImage: '/products/teacher-planner-hero.png',  // ضع مسار صورتك
  
  // معرض الصور
  images: [
    '/products/planner-1.png',  // غيّر هذه بصور منتجك
    '/products/planner-2.png',
    '/products/planner-3.png',
    '/products/planner-4.png',
  ],
}
```

### المميزات الرئيسية:

```typescript
features: [
  {
    icon: '📅',  // أي إيموجي
    title_ar: 'تخطيط أسبوعي شامل',
    title_en: 'Comprehensive Weekly Planning',
    desc_ar: 'خطط لكل دروسك الأسبوعية في مكان واحد منظم',
    desc_en: 'Plan all your weekly lessons in one organized place'
  },
  // أضف المزيد...
]
```

### ما يتضمنه المنتج:

```typescript
includes: [
  'صفحات تخطيط أسبوعي (52 أسبوع)',  // غيّر هذه القائمة
  'قوالب تحضير الدروس',
  'متتبع الحضور والغياب',
  // أضف المزيد...
]
```

### المواصفات:

```typescript
specs: {
  size: 'A4 (21 × 29.7 سم)',
  pages: '120 صفحة',
  paper: 'ورق فاخر 100 جرام',
  binding: 'تجليد حلزوني',
  cover: 'غلاف صلب مقوى'
}
```

---

## 💾 2. صفحة المنتج الرقمي (DigitalProductLanding)

### البيانات الأساسية:

```typescript
const PRODUCT_DATA = {
  title_ar: 'قوالب تحضير الدروس الرقمية',
  price: 500,  // عادة أرخص من المطبوع
  
  // معلومات الملف
  fileInfo: {
    format: 'Word (.docx) + PDF',
    size: '25 MB',
    files: '35 ملف',
    compatibility: 'Windows, Mac, Mobile',
    language: 'عربي + إنجليزي'
  },
}
```

### الفوائد:

```typescript
benefits: [
  {
    title_ar: 'وفّر وقتك',
    desc_ar: 'بدلاً من قضاء ساعات في التصميم، استخدم قوالب جاهزة وركز على التدريس'
  },
  // أضف المزيد...
]
```

### الأسئلة الشائعة:

```typescript
faq: [
  {
    q: 'كيف أحصل على المنتج بعد الشراء؟',
    a: 'ستحصل على رابط تحميل فوري بعد إتمام عملية الدفع عبر البريد الإلكتروني'
  },
  // أضف المزيد...
]
```

---

## 🎓 3. صفحة الدورة (CourseLanding)

### البيانات الأساسية:

```typescript
const COURSE_DATA = {
  title_ar: 'دورة إتقان Canva للمعلمين',
  price: 3000,
  isFree: false,  // غيّر إلى true للدورات المجانية
  
  // إحصائيات الدورة
  stats: {
    duration: '12 ساعة',
    lessons: '45 درس',
    students: '500+ طالب',
    level: 'مبتدئ إلى متقدم',
    language: 'عربي',
    certificate: true
  },
}
```

### نتائج التعلم:

```typescript
outcomes: [
  'إتقان واجهة Canva وجميع أدواتها',
  'تصميم عروض تقديمية احترافية للدروس',
  'إنشاء أوراق عمل وأنشطة تفاعلية',
  // أضف المزيد...
]
```

### منهج الدورة:

```typescript
curriculum: [
  {
    module: 'الوحدة الأولى: البداية مع Canva',
    lessons: [
      { 
        title: 'مقدمة عن Canva وإنشاء حساب', 
        duration: '10 دقائق', 
        free: true  // الدروس المجانية للمعاينة
      },
      { 
        title: 'جولة في واجهة Canva', 
        duration: '15 دقائق', 
        free: true 
      },
      // أضف المزيد...
    ]
  },
  // أضف وحدات أخرى...
]
```

### معلومات المدرب:

```typescript
instructor: {
  name: 'أستاذة سارة أحمد',
  title: 'مصممة تعليمية ومدربة Canva معتمدة',
  bio: 'خبرة 8 سنوات في التصميم التعليمي...',
  avatar: '👩‍🏫',  // أو ضع مسار صورة
  stats: {
    students: '5000+',
    courses: '12',
    rating: 4.9
  }
}
```

---

## 🖼️ كيفية إضافة الصور

### الطريقة 1: استخدام مجلد public

1. ضع صورك في مجلد `public/products/` أو `public/courses/`
2. استخدم المسار مباشرة: `/products/my-image.png`

### الطريقة 2: استخدام مجلد assets

1. ضع الصور في `src/assets/`
2. استورد الصورة:
```typescript
import myImage from '../assets/my-image.png';
```
3. استخدمها: `heroImage: myImage`

### نصيحة: حجم الصور المثالي

- **صورة البطل (Hero)**: 800×600 بكسل
- **صور المعرض**: 600×400 بكسل
- **الصور المصغرة**: 200×200 بكسل

---

## 🎨 تخصيص الألوان

الصفحات تستخدم ألوان NawaEduTech تلقائياً:

- البنفسجي الأساسي: `#340690`
- البنفسجي الثانوي: `#5f2cc7`
- البنفسجي الفاتح: `#864bf5`
- الذهبي: `#f3b942`

إذا أردت تغيير الألوان، ابحث عن:
- `bg-[#340690]` - لون الخلفية
- `text-[#340690]` - لون النص
- `border-[#340690]` - لون الحدود

---

## 👥 إضافة آراء العملاء

```typescript
testimonials: [
  {
    name: 'أستاذة سارة',
    role: 'معلمة ابتدائي',
    rating: 5,  // من 1 إلى 5
    text: 'المخطط ساعدني كثيراً في تنظيم وقتي...',
    avatar: '👩‍🏫'  // إيموجي أو مسار صورة
  },
  // أضف المزيد...
]
```

---

## 🔗 ربط الصفحات بقاعدة البيانات

حالياً الصفحات تستخدم بيانات ثابتة. لربطها بقاعدة البيانات:

### الخطوة 1: أنشئ صفحة ديناميكية

```typescript
// مثال: src/pages/ProductLanding.tsx
import { useParams } from 'react-router-dom';
import { useEffect, useState } from 'react';
import { supabase } from '../lib/supabase';

export function ProductLanding() {
  const { id } = useParams();
  const [product, setProduct] = useState(null);
  
  useEffect(() => {
    loadProduct();
  }, [id]);
  
  const loadProduct = async () => {
    const { data } = await supabase
      .from('products')
      .select('*')
      .eq('id', id)
      .single();
    setProduct(data);
  };
  
  // استخدم بيانات product بدلاً من PRODUCT_DATA
}
```

### الخطوة 2: أضف Route ديناميكي

في `App.tsx`:
```typescript
<Route path="/product/:id" element={<ProductLanding />} />
```

---

## 📱 التأكد من التصميم المتجاوب

الصفحات متجاوبة تلقائياً، لكن تأكد من:

1. **اختبر على الهاتف**: افتح DevTools واختر عرض الهاتف
2. **تأكد من الصور**: يجب أن تكون واضحة على جميع الأحجام
3. **النصوص**: يجب أن تكون قابلة للقراءة

---

## ✅ قائمة التحقق قبل النشر

- [ ] تم تعديل جميع النصوص
- [ ] تم رفع جميع الصور
- [ ] تم اختبار الصفحة على الهاتف والكمبيوتر
- [ ] تم التأكد من الأسعار
- [ ] تم اختبار الأزرار (Add to Cart, Enroll, etc.)
- [ ] تم مراجعة الأخطاء الإملائية
- [ ] تم التأكد من الترجمة الإنجليزية (إذا لزم الأمر)

---

## 🆘 حل المشاكل الشائعة

### المشكلة: الصور لا تظهر
**الحل**: تأكد من:
1. المسار صحيح (`/products/image.png`)
2. الصورة موجودة في مجلد `public`
3. اسم الملف بدون مسافات أو أحرف خاصة

### المشكلة: التصميم لا يظهر
**الحل**: 
1. امسح الـ cache: `Ctrl + Shift + R`
2. تأكد من تشغيل السيرفر
3. تحقق من console للأخطاء

### المشكلة: النصوص بالإنجليزية
**الحل**: تأكد من تعديل `title_ar` و `desc_ar` وليس فقط النسخة الإنجليزية

---

## 💡 نصائح للحصول على أفضل نتيجة

1. **استخدم صور عالية الجودة**: صور واضحة تزيد المبيعات
2. **اكتب أوصاف مقنعة**: ركز على الفوائد وليس المميزات فقط
3. **أضف آراء حقيقية**: آراء العملاء تزيد الثقة
4. **حدّث الأسعار بانتظام**: تأكد من تطابق الأسعار مع المتجر
5. **اختبر الأزرار**: تأكد من عمل جميع الأزرار بشكل صحيح

---

## 📞 الدعم

إذا واجهت أي مشكلة:
1. راجع هذا الدليل أولاً
2. تحقق من console للأخطاء
3. تأكد من تحديث الصفحة بعد التعديلات

---

## 🎉 مثال عملي كامل

### إنشاء صفحة لمنتج جديد:

1. **انسخ ملف القالب المناسب**
2. **عدّل PRODUCT_DATA**:

```typescript
const PRODUCT_DATA = {
  title_ar: 'دفتر التحضير الذكي',
  title_en: 'Smart Preparation Notebook',
  price: 1200,
  originalPrice: 1500,
  heroImage: '/products/smart-notebook.png',
  images: [
    '/products/notebook-1.png',
    '/products/notebook-2.png',
    '/products/notebook-3.png',
  ],
  features: [
    {
      icon: '📝',
      title_ar: 'تصميم عملي',
      desc_ar: 'مصمم خصيصاً لاحتياجات المعلم اليومية'
    },
    // المزيد...
  ],
  // بقية البيانات...
}
```

3. **احفظ الملف**
4. **افتح المتصفح وشاهد النتيجة!**

---

**تم إنشاء هذا الدليل بواسطة NawaEduTech** 💜✨
